# Git Workflow Rules

- **Branching Policy**: NEVER commit directly to the `main` branch. 
- **Workflow**: Always create a new feature/fix branch (e.g. `feature/your-feature-name`), make your changes and commits there, and then merge the branch into `main` (either locally or via a Pull Request).
